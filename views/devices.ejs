<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <title>Gerenciar Dispositivos</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
    />
  </head>
  <body class="dashboard">
    <%- include("partials/navbar") %>

    <main class="container">
      <div class="device-header">
        <button class="device-button" id="openDeviceModal">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="white"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <line x1="12" y1="5" x2="12" y2="19" />
            <line x1="5" y1="12" x2="19" y2="12" />
          </svg>
          <span>Novo Dispositivo</span>
        </button>
      </div>

      <div class="device-table-wrapper">
        <table class="device-table">
          <thead>
            <tr>
              <th>Nome</th>
              <th>Tipo</th>
              <th>Status</th>
              <th>Último Acesso</th>
              <th>Dados de Acesso</th>
            </tr>
          </thead>
          <tbody>
            <% if (devices.length === 0) { %>
            <tr>
              <td colspan="5" style="text-align: center">
                Nenhum dispositivo cadastrado.
              </td>
            </tr>
            <% } else { %> <% devices.forEach(device => { %>
            <tr>
              <td><%= device.name %></td>
              <td class="icon-cell">
                <% if (device.device_type === 'tv') { %>
                <i
                  class="bi bi-tv"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else if (device.device_type === 'windows') { %>
                <i
                  class="bi bi-windows"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else if (device.device_type === 'linux') { %>
                <i
                  class="bi bi-terminal"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else if (device.device_type === 'android') { %>
                <i
                  class="bi bi-android2"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else if (device.device_type === 'ios') { %>
                <i
                  class="bi bi-apple"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else if (device.device_type === 'web') { %>
                <i
                  class="bi bi-globe2"
                  style="color: var(--color-primary); font-size: 1rem"
                ></i>
                <% } else { %>
                <i
                  class="bi bi-question-circle"
                  style="color: #999; font-size: 1rem"
                ></i>
                <% } %>
              </td>

              <td>
                <span
                  class="status <%= device.is_active ? 'status-ativo' : 'status-inativo' %>"
                >
                  <%= device.is_active ? 'Ativo' : 'Inativo' %>
                </span>
              </td>
              <td><%= device.last_seen_formatted %></td>
              <td>
                <button
                  class="action-icon show-connection"
                  data-identifier="<%= device.device_identifier %>"
                  data-key="<%= device.authentication_key %>"
                  style="color: var(--color-primary)"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="18"
                    height="18"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path
                      d="M17 11V7a5 5 0 0 0-10 0v4m12 0H5a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-6a2 2 0 0 0-2-2Z"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                    <circle cx="12" cy="16" r="1" />
                  </svg>
                </button>
              </td>
            </tr>
            <% }) %> <% } %>
          </tbody>
        </table>
      </div>
    </main>

    <div id="deviceModal" class="modal-overlay">
      <div class="modal-card">
        <div class="modal-header centered">
          <h2 class="modal-title centered">Cadastrar Novo Dispositivo</h2>
        </div>
        <form action="/devices" method="POST" class="modal-form">
          <input
            type="text"
            name="name"
            placeholder="Nome do Dispositivo"
            required
            class="modal-input"
          />
          <select name="device_type" class="modal-input" required>
            <option value="tv">TV</option>
            <option value="windows">Windows</option>
            <option value="linux">Linux</option>
            <option value="android">Android</option>
            <option value="ios">iOS</option>
            <option value="web">Web</option>
            <option value="unknown" selected>Outro</option>
          </select>
          <div class="modal-actions">
            <button type="submit" class="device-button">Adicionar</button>
            <button
              type="button"
              id="cancelDeviceModal"
              class="device-button device-button-cancel"
            >
              Cancelar
            </button>
          </div>
        </form>
      </div>
    </div>

    <div id="connectionModal" class="modal-overlay">
      <div class="modal-card">
        <div class="modal-header centered">
          <h3 class="modal-title centered">Dados de Acesso</h3>
        </div>
        <div class="modal-form connection-modal-form">
          <div class="connection-input-group">
            <i class="bi bi-cpu"></i>
            <input type="text" id="modalDeviceId" readonly />
          </div>
          <div class="connection-input-group">
            <i class="bi bi-key"></i>
            <input type="text" id="modalDeviceKey" readonly />
          </div>
          <div class="modal-actions centered">
            <button
              type="button"
              class="device-button device-button-cancel"
              id="closeConnectionModal"
            >
              Fechar
            </button>
          </div>
        </div>
      </div>
    </div>

    <%- include("partials/footer") %>
    <script src="/js/devices.js"></script>
  </body>
</html>
